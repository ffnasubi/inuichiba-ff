# .firebaserc（設定ガイド）

## ✅ このファイルは Firebase CLI に読み込まれる JSON の定義です。
本番用の `.firebaserc` に書き込む内容をここにテンプレートとして記述しています。

---

## 🔹 基本構成

- `default` は **開発環境(ffdev)** を指定（事故防止）
- `ffdev` / `ffprod` の短縮名も併記することで `--project=ffdev` が使える

```json
{
  "projects": {
    "default": "inuichiba-ffdev",
    "ffprod": "inuichiba-ffprod",
    "ffdev": "inuichiba-ffdev"
  }
}

---

## 上記のような.firebasercの定義により、デプロイコマンドは次のように簡略化されます
 - <開発環境>
firebase deploy --only functions --force
firebase deploy --only functions --project=ffdev --force
 - <本番環境>
firebase deploy --only functions --project=ffprod --force

---

## ✅ 運用まとめ

| ファイル名        | 役割                                                |
|------------------|-----------------------------------------------------|
| `.firebaserc`    | Firebase CLI に読み込まれる本番用 JSON（コメントなし） |
| `.firebaserc.md` | **人間向けの設定ガイド**・後継者への説明書として最適    |


